/**
 * Command-line front-end of the simulation.
 *
 * <p>Contains the {@code CliRunner} class, which exposes a small interactive
 * shell that drives a {@link com.cellsimulation.model.SimulationEngine}. The
 * package is fully independent from JavaFX so that the simulation can be
 * demonstrated on any machine with only a JDK installed.
 */
package com.cellsimulation.cli;
